|   | IP Address| MAC Address |
| ----- | ---- | --- |
|   Structure   | IPv4 written in decimal separated by periods(32bit),IPv6 written in hexadecimal separated by colons(128bit).      |MAC written in hexadecimal separated by daches(48bit)    |
|    usage  | Identifies devices on the Internet or large networks and routing data between networks| find a device in the same local network and delivering data between nearby devices|
|   Location in the network layers   |Network layer      |Data Link layer|
